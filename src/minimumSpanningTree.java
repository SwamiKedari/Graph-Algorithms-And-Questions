public class minimumSpanningTree {
    // the spanning tree is a graph which has exactly n vertices and n-1 edges
    // the minimum spanning tree is a graph which has the n vertices  , n-1 edges and also has the minimum value of the all the weights
    // the 2 major algorithms for finding the minimum spanning tree is the prim's algorithm and the kruskal's algorithms


}
